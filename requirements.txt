# these libs are requested to build application env
#
# Production builds (Dockerfile) install from requirements.lock with
# --require-hashes. After editing this file, regenerate the lock with:
#
#   uv pip compile requirements.txt --python-version 3.12 \
#     --generate-hashes -o requirements.lock
#
# Then commit both files in the same change.

solara>=1.50.0 
uvicorn<0.30
starlette<0.44
ee-client>=2.4.0

toml

ipecharts>=1.2.0
rasterio<1.3.11 # Leave it here even if it's already in the sepal_ui requirements. Check https://github.com/openforis/sepal/issues/328

pysepal>=3.6.1

https://github.com/dfguerrerom/ipyleaflet/raw/634bcf22c09034a917819437358d5840fa72ac41/wheels/jupyter_leaflet-0.20.0-py3-none-any.whl
https://github.com/dfguerrerom/ipyleaflet/raw/634bcf22c09034a917819437358d5840fa72ac41/wheels/ipyleaflet-0.20.0-py3-none-any.whl

# custom libs
earthengine-api>=1.6
pandas
Shapely
matplotlib
geopandas
numpy
numexpr

# even though it will installed by sepal_ui, I want to point out that it is required by the app
pygaul

# For testing
pytest
pytest-cov

# for development
colorlog